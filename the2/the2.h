#ifndef HEADER_FILE_NAME
#define HEADER_FILE_NAME

#include <string>

int radix_string_sort(std::string* arr, int size, bool ascending);

int counting_sort(std::string *arr, int startIndex, int endIndex, int size, int index, bool ascending);

#endif